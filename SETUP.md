# ProTracker V24

Replace the deployed `index.html` with this version. Then hard-refresh the browser (Cmd+Shift+R).

Important: this version assumes the real hierarchy is stored in `uplineUid`. If a user has no valid `uplineUid`, they will not be included in the Team Dashboard until the hierarchy is corrected in Admin.
